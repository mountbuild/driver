
fetch @seed/tree/force/state
  catch force, store force

write js
  field force, state force
  field store

  write wheel
    hitch wheel, share slate
    catch stack
      write state, state stack
        hitch stack, share stack

write stack
  field stack
  write state, state verse
    hitch stack, share stack
  write state, state slate
    hitch stack, share stack

write verse

write slate
  write wheel
    hitch wheel, share stack, field
    catch stack, store field
      write state, state field
      write state, state blank

write field
write blank
