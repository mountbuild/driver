force get-ssid
  field build, field ne-hotspot-configuration

  cause get-aspect
    drive object, share build
    drive aspect, |ssid|
    raise build, field string

force set-ssid
  field build, field ne-hotspot-configuration
  field value, field string

  cause set-aspect
    drive object, share build
    drive aspect, |ssid|
    drive factor, share value

force get-ssid-prefix
  field build, field ne-hotspot-configuration

  cause get-aspect
    drive object, share build
    drive aspect, |ssidPrefix|
    raise build, field string

force set-ssid-prefix
  field build, field ne-hotspot-configuration
  field value, field string

  cause set-aspect
    drive object, share build
    drive aspect, |ssidPrefix|
    drive factor, share value

force get-life-time-in-days
  field build, field ne-hotspot-configuration

  cause get-aspect
    drive object, share build
    drive aspect, |lifeTimeInDays|
    raise build, field ns-number

force set-life-time-in-days
  field build, field ne-hotspot-configuration
  field value, field ns-number

  cause set-aspect
    drive object, share build
    drive aspect, |lifeTimeInDays|
    drive factor, share value

force get-join-once
  field build, field ne-hotspot-configuration

  cause get-aspect
    drive object, share build
    drive aspect, |joinOnce|
    raise build, field bool

force set-join-once
  field build, field ne-hotspot-configuration
  field value, field bool

  cause set-aspect
    drive object, share build
    drive aspect, |joinOnce|
    drive factor, share value

force get-hidden
  field build, field ne-hotspot-configuration

  cause get-aspect
    drive object, share build
    drive aspect, |hidden|
    raise build, field bool

force set-hidden
  field build, field ne-hotspot-configuration
  field value, field bool

  cause set-aspect
    drive object, share build
    drive aspect, |hidden|
    drive factor, share value
