force get-bit-count
  field build, field symmetric-key

  cause get-aspect
    drive object, share build
    drive aspect, |bitCount|
    raise build, field int

force set-bit-count
  field build, field symmetric-key
  field value, field int

  cause set-aspect
    drive object, share build
    drive aspect, |bitCount|
    drive factor, share value
