
force build-key-rep
  mount type, field key-rep-type
  mount algorithm, field string
  mount format, field string
  mount encoded, field byte-array

  cause invoke-constructor
    mount constructor, |KeyRep|
    mount factor, share type
    mount factor, share algorithm
    mount factor, share format
    mount factor, share encoded
    throw build, field key-rep
