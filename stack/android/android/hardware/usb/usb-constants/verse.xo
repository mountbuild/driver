
force get-usb-class-app-spec
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_APP_SPEC|
    throw build, field int

force get-usb-class-audio
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_AUDIO|
    throw build, field int

force get-usb-class-cdc-data
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_CDC_DATA|
    throw build, field int

force get-usb-class-comm
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_COMM|
    throw build, field int

force get-usb-class-content-sec
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_CONTENT_SEC|
    throw build, field int

force get-usb-class-cscid
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_CSCID|
    throw build, field int

force get-usb-class-hid
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_HID|
    throw build, field int

force get-usb-class-hub
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_HUB|
    throw build, field int

force get-usb-class-mass-storage
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_MASS_STORAGE|
    throw build, field int

force get-usb-class-misc
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_MISC|
    throw build, field int

force get-usb-class-per-interface
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_PER_INTERFACE|
    throw build, field int

force get-usb-class-physica
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_PHYSICA|
    throw build, field int

force get-usb-class-printer
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_PRINTER|
    throw build, field int

force get-usb-class-still-image
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_STILL_IMAGE|
    throw build, field int

force get-usb-class-vendor-spec
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_VENDOR_SPEC|
    throw build, field int

force get-usb-class-video
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_VIDEO|
    throw build, field int

force get-usb-class-wireless-controller
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_CLASS_WIRELESS_CONTROLLER|
    throw build, field int

force get-usb-dir-in
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_DIR_IN|
    throw build, field int

force get-usb-dir-out
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_DIR_OUT|
    throw build, field int

force get-usb-endpoint-dir-mask
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_DIR_MASK|
    throw build, field int

force get-usb-endpoint-number-mask
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_NUMBER_MASK|
    throw build, field int

force get-usb-endpoint-xfertype-mask
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_XFERTYPE_MASK|
    throw build, field int

force get-usb-endpoint-xfer-bulk
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_XFER_BULK|
    throw build, field int

force get-usb-endpoint-xfer-control
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_XFER_CONTROL|
    throw build, field int

force get-usb-endpoint-xfer-int
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_XFER_INT|
    throw build, field int

force get-usb-endpoint-xfer-isoc
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_ENDPOINT_XFER_ISOC|
    throw build, field int

force get-usb-interface-subclass-boot
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_INTERFACE_SUBCLASS_BOOT|
    throw build, field int

force get-usb-subclass-vendor-spec
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_SUBCLASS_VENDOR_SPEC|
    throw build, field int

force get-usb-type-class
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_TYPE_CLASS|
    throw build, field int

force get-usb-type-mask
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_TYPE_MASK|
    throw build, field int

force get-usb-type-reserved
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_TYPE_RESERVED|
    throw build, field int

force get-usb-type-standard
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_TYPE_STANDARD|
    throw build, field int

force get-usb-type-vendor
  mount build, field usb-constants

  cause get-property
    drive object, share build
    drive property, |USB_TYPE_VENDOR|
    throw build, field int

force build-usb-constants
  cause invoke-constructor
    drive constructor, |UsbConstants|
    throw build, field usb-constants
