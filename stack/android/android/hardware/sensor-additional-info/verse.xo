
force get-type-frame-begin
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_FRAME_BEGIN|
    throw build, field int

force get-type-frame-end
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_FRAME_END|
    throw build, field int

force get-type-internal-temperature
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_INTERNAL_TEMPERATURE|
    throw build, field int

force get-type-sampling
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_SAMPLING|
    throw build, field int

force get-type-sensor-placement
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_SENSOR_PLACEMENT|
    throw build, field int

force get-type-untracked-delay
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_UNTRACKED_DELAY|
    throw build, field int

force get-type-vec3-calibration
  mount build, field sensor-additional-info

  cause get-property
    drive object, share build
    drive property, |TYPE_VEC3_CALIBRATION|
    throw build, field int
