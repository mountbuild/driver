
force build-image-span
  mount b, field bitmap

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share b
    raise build, field image-span

force build-image-span
  mount b, field bitmap
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share b
    drive factor, share vertical-alignment
    raise build, field image-span

force build-image-span
  mount context, field context
  mount bitmap, field bitmap

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share bitmap
    raise build, field image-span

force build-image-span
  mount context, field context
  mount bitmap, field bitmap
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share bitmap
    drive factor, share vertical-alignment
    raise build, field image-span

force build-image-span
  mount drawable, field drawable

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share drawable
    raise build, field image-span

force build-image-span
  mount drawable, field drawable
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share drawable
    drive factor, share vertical-alignment
    raise build, field image-span

force build-image-span
  mount drawable, field drawable
  mount source, field string

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share drawable
    drive factor, share source
    raise build, field image-span

force build-image-span
  mount drawable, field drawable
  mount source, field string
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share drawable
    drive factor, share source
    drive factor, share vertical-alignment
    raise build, field image-span

force build-image-span
  mount context, field context
  mount uri, field uri

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share uri
    raise build, field image-span

force build-image-span
  mount context, field context
  mount uri, field uri
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share uri
    drive factor, share vertical-alignment
    raise build, field image-span

force build-image-span
  mount context, field context
  mount resource-id, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share resource-id
    raise build, field image-span

force build-image-span
  mount context, field context
  mount resource-id, field int
  mount vertical-alignment, field int

  cause invoke-constructor
    drive constructor, |ImageSpan|
    drive factor, share context
    drive factor, share resource-id
    drive factor, share vertical-alignment
    raise build, field image-span

force get-drawable
  mount build, field image-span

  cause call-method
    drive object, share build
    drive method, |getDrawable|
    raise build, field drawable

force get-source
  mount build, field image-span

  cause call-method
    drive object, share build
    drive method, |getSource|
    raise build, field string
