
force build-currency-amount
  mount number, field number
  mount currency, field currency

  cause invoke-constructor
    drive constructor, |CurrencyAmount|
    drive factor, share number
    drive factor, share currency
    raise build, field currency-amount

force build-currency-amount
  mount number, field double
  mount currency, field currency

  cause invoke-constructor
    drive constructor, |CurrencyAmount|
    drive factor, share number
    drive factor, share currency
    raise build, field currency-amount

force build-currency-amount
  mount number, field number
  mount currency, field currency

  cause invoke-constructor
    drive constructor, |CurrencyAmount|
    drive factor, share number
    drive factor, share currency
    raise build, field currency-amount

force build-currency-amount
  mount number, field double
  mount currency, field currency

  cause invoke-constructor
    drive constructor, |CurrencyAmount|
    drive factor, share number
    drive factor, share currency
    raise build, field currency-amount

force get-currency
  mount build, field currency-amount

  cause call-method
    drive object, share build
    drive method, |getCurrency|
    raise build, field currency
