
force build-sweep-gradient
  mount cx, field float
  mount cy, field float
  mount colors, field int-array
  mount positions, field float-array

  cause invoke-constructor
    drive constructor, |SweepGradient|
    drive factor, share cx
    drive factor, share cy
    drive factor, share colors
    drive factor, share positions
    raise build, field sweep-gradient

force build-sweep-gradient
  mount cx, field float
  mount cy, field float
  mount colors, field long-array
  mount positions, field float-array

  cause invoke-constructor
    drive constructor, |SweepGradient|
    drive factor, share cx
    drive factor, share cy
    drive factor, share colors
    drive factor, share positions
    raise build, field sweep-gradient

force build-sweep-gradient
  mount cx, field float
  mount cy, field float
  mount color0, field int
  mount color1, field int

  cause invoke-constructor
    drive constructor, |SweepGradient|
    drive factor, share cx
    drive factor, share cy
    drive factor, share color0
    drive factor, share color1
    raise build, field sweep-gradient

force build-sweep-gradient
  mount cx, field float
  mount cy, field float
  mount color0, field long
  mount color1, field long

  cause invoke-constructor
    drive constructor, |SweepGradient|
    drive factor, share cx
    drive factor, share cy
    drive factor, share color0
    drive factor, share color1
    raise build, field sweep-gradient
