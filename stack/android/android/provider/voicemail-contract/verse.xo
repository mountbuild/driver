
force get-action-fetch-voicemail
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |ACTION_FETCH_VOICEMAIL|
    raise build, field string

force get-action-new-voicemail
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |ACTION_NEW_VOICEMAIL|
    raise build, field string

force get-action-sync-voicemail
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |ACTION_SYNC_VOICEMAIL|
    raise build, field string

force get-authority
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |AUTHORITY|
    raise build, field string

force get-extra-phone-account-handle
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |EXTRA_PHONE_ACCOUNT_HANDLE|
    raise build, field string

force get-extra-self-change
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |EXTRA_SELF_CHANGE|
    raise build, field string

force get-param-key-source-package
  mount build, field voicemail-contract

  cause get-property
    drive object, share build
    drive property, |PARAM_KEY_SOURCE_PACKAGE|
    raise build, field string
